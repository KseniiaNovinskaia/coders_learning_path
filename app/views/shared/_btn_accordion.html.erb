<a href=<%= locals[:url] %>, target="_blank", class="btn-accordion-link-wrapper">
  <div class="btn-accordion">
    <div class="btn-accordion-text">
      <% if locals[:completed] %>
        Start again
      <% else %>
        Start
      <% end %>
    </div>
  </div>
</a>
